# Role System Integration TODO

Status: Planning approved, starting implementation.

## Steps from Approved Plan:

### Backend (User Responsibility):
- [ ] Create `roles` table (id, name)
- [ ] Add `role_id` to `users` table (foreign key)
- [ ] API: GET `/api/roles` (list)
- [ ] Update `/api/register` & `/api/users` to handle `role_id`


3. [ ] **Optional NavbarComponent.jsx**: Display user role if backend returns it
4. [ ] Test & complete

Current Step: 1/4

